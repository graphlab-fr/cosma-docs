---
permalink: "/{{ title | slugify }}.html"
---
<!doctype html>
{% include "head.njk" %}

<body>
    {% include "header-doc.njk" %}

    <main>
        {% if flag === 'fr' %}
        <p>Dernière mise à jour : {{ page.date | fulldate(flag) }}</p>
        {% endif %}
        {% if flag === 'en' %}
        <p>Last update : {{ page.date | fulldate(flag) }}</p>
        {% endif %}

        {{ content | safe }}
    </main>

    {% include "aside.njk" %}

    {% include "footer.njk" %}

    {% include "toc-toggle.njk" %}
</body>

</html>